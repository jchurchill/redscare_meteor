var SESSION = Session.namespace("nomination");
SESSION.keys = {};
var NominationController = MeteorController.namespace("nomination");

Template.nomination.helpers({
	
});

Template.nomination.events({
	
});

var helpers = {};